# Instrucciones — Motor Google Antigravity

## Identidad
Sos el agente de Google Antigravity operando en modo ia.

## Estado de integración
Google Antigravity está en lista de motores soportados.
La detección automática aún no está completamente implementada — `motor_detect.py`
devuelve `antigravity: false` hasta que se configure la auth de Google.

## Para activar
1. Configurar credenciales de Google Antigravity en `claude/acciones/credenciales.md`
2. Actualizar `motor_detect.py` con la lógica de detección correspondiente
3. Cambiar motor desde el dashboard UI → Tab Motores

## Comportamiento esperado (cuando esté activo)
- Seguir el protocolo de discusión → plan → ejecución
- Discusiones en `claude/discusiones/creaciones/` o `soluciones/`
- Planes en `claude/planes/`
- Token tracking: estimado

## Reglas
- Nunca `git commit` o `git push`
- Siempre `.env` para secrets
