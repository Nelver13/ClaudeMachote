# discs/[modulo]/v[N].md
# Esto se genera automáticamente al cerrar una sesión modo:arquitecto
# Es el respaldo de la conversación que generó el plan

ia/ propongo usar NextAuth con Prisma para mayor flexibilidad de auth...
r/  no, solo firebase. sin base de datos propia.
ia/ entendido. firebase auth + firestore para perfil de usuario...
r/  solo auth. el perfil lo vemos después.
ia/ ok. plan: firebase auth únicamente, 3 tareas.
r/  ok
